import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cloud.molex.app',
  appName: 'molexee',
  webDir: 'dist/molexee',
  bundledWebRuntime: false
};

export default config;
